// src/app/api/learn/progress/[courseId]/[lessonId]/route.ts
// Contenido se añadirá en el siguiente paso.
import { NextResponse } from 'next/server';

export async function POST() {
  return NextResponse.json({ message: 'Endpoint POST para marcar lección como completada (no implementado aún)' });
}
