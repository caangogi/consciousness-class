// src/app/api/learn/progress/[courseId]/route.ts
// Contenido se añadirá en el siguiente paso.
import { NextResponse } from 'next/server';

export async function GET() {
  return NextResponse.json({ message: 'Endpoint GET para progreso del curso (no implementado aún)' });
}
