
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // WARNING: These rules allow anyone to read and write to your storage.
      // Only for temporary debugging. Revert to secure rules ASAP.
      allow read, write: if true;
    }
  }
}
